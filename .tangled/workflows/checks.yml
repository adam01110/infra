when:
  - event: ["push", "manual"]
    branch: ["main"]
  - event: ["pull_request"]
    branch: ["main"]
engine: "nixery"
dependencies:
  nixpkgs:
    - nix
steps:
  - name: "Run checks"
    command: |
      set -euo pipefail
      nix --extra-experimental-features pipe-operators flake check
